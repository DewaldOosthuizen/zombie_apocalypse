[gd_scene format=3 uid="uid://csg3ln8ff4k3x"]

[ext_resource type="Texture2D" uid="uid://vlak8eqwks1e" path="res://resources/hud/health/Health1.png" id="1"]
[ext_resource type="Texture2D" uid="uid://c4gljq8ylj0bh" path="res://resources/hud/power/Power1.png" id="2"]
[ext_resource type="PackedScene" path="res://scenes/gui/PopupPanel.tscn" id="3"]

[sub_resource type="GDScript" id="1"]
script/source = "extends Node2D

var active_level = 0 # starting level
var ninja_scene = preload(\"res://scenes/characters/ninja/Character_Scene.tscn\")
var robot_scene = preload(\"res://scenes/characters/robot/Character_Scene.tscn\")
var ranger_scene = preload(\"res://scenes/characters/ranger/Character_Scene.tscn\")

var playable_levels = [
	preload(\"res://scenes/levels/Level_1_Scene.tscn\"),
	preload(\"res://scenes/levels/Level_2_Scene.tscn\")
]

var character_selection = 0
var characters = [
	{\"type\": \"ninja\", \"gender\": \"female\", \"scene\": ninja_scene}, 
	{\"type\": \"ninja\", \"gender\": \"male\", \"scene\": ninja_scene},
	{\"type\": \"robot\", \"gender\": \"male\", \"scene\": robot_scene},
	{\"type\": \"ranger\", \"gender\": \"female\", \"scene\": ranger_scene}, 
	{\"type\": \"ranger\", \"gender\": \"male\", \"scene\": ranger_scene},
	{\"type\": \"dino\", \"gender\": \"\", \"scene\": dino_scene}
]

var character_stats = {
	\"loaded\": false,
	\"health\": 0,
	\"energy\": 0,
	\"ammo\": 0,
	\"character\": characters[character_selection]
} # player character attributes

var character_node = null

@onready var camera = load(\"res://scenes/characters/Camera2D.tscn\").instantiate()

# Called when the node enters the scene tree for the first time.
func _ready():
	# set the game to full screen
	DisplayServer.window_set_mode(DisplayServer.WINDOW_MODE_FULLSCREEN)
	DisplayServer.screen_set_keep_on(true)
	# start the first level
	advance_to_next_level()


#Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	# get player input
	handle_events()
	_connect_signals()
	

func _connect_signals():
	# get the end door for current level and listen for events, if main character enters the door
	# the signal will emit and the next level will be created
	var end_door = get_tree().get_nodes_in_group(\"end_door\")
	if (!end_door.is_empty()):
		end_door = end_door.front()
		# connect end door signals
		if(!end_door.is_connected(\"enteredEndDoor\", Callable(self, \"advance_to_next_level\"))):
			end_door.connect(\"enteredEndDoor\", Callable(self, \"advance_to_next_level\"))
			print(\":: end door connected ::\")
			
	var found = get_tree().get_nodes_in_group(\"main_character\")
	if (!found.is_empty()):
		character_node = found.front()
		_connect_to_character_signals(character_node)
	# character_node is NOT touched when the group is empty — preserves the valid node reference


func handle_events():
	if (Input.is_action_just_pressed(\"ui_cancel\")):
		if (get_tree().paused):
			get_tree().paused = false
		else:
			get_tree().paused = true
	elif (Input.is_action_just_pressed(\"respawn\")):
		if (character_stats[\"health\"] > 0):
			setup_character()
	elif (Input.is_action_just_pressed(\"tab\")):
		character_selection += 1
		
		if (character_selection > characters.size() -1):
			character_selection = 0
		
		# set new selected character
		character_stats[\"character\"] = characters[character_selection]
			
		# create new character
		var newCharacter = (character_stats[\"character\"][\"scene\"]).instantiate()
		newCharacter.position = character_node.position
		character_node.remove_child(camera)
		character_node.queue_free()
	
		newCharacter.gender = character_stats[\"character\"][\"gender\"]
		_connect_to_character_signals(newCharacter)
		camera.enabled = true
		newCharacter.add_child(camera)
		get_tree().root.add_child(newCharacter)


func advance_to_next_level():
	print(\"== start next level ==\")
	# remove previous level if exists
	var active_level_scene = get_tree().get_nodes_in_group(\"level\")
	for l in active_level_scene:
		if (l):
			l.queue_free()
	
	# remove all items spawed in previous level if exists
	var power_ups = get_tree().get_nodes_in_group(\"power_up\")
	for l in power_ups:
		if (l):
			l.queue_free()

	# remove all enemies spawed in previous level if exists
	var enemies = get_tree().get_nodes_in_group(\"enemy_character\")
	for l in enemies:
		if (l):
			l.queue_free()
	
	# remove all bones spawed in previous level if exists
	var bones = get_tree().get_nodes_in_group(\"bones\")
	for l in bones:
		if (l):
			l.queue_free()
	
	# increment level and add next level as active scene
	active_level += 1;
	create_level()


func create_level():
	print(\"== create level ==\")
	var level_scene = playable_levels[active_level - 1]
	if (level_scene):
		level_scene = level_scene.instantiate()
		if (!level_scene.is_connected(\"level_entered\", Callable(self, \"setup_level\"))):
			level_scene.connect(\"level_entered\", Callable(self, \"setup_level\"))
#		if (!level_scene.is_connected(\"exit_level\", self, \"reset_current_scene\")):
#			level_scene.connect(\"exit_level\", self, \"reset_current_scene\")
		
		# set the next level as the current scene
		get_tree().root.call_deferred(\"add_child\", level_scene)
		print(\":: started level \" + str(active_level) + \" ::\")


func reset_current_scene(character):
	print(\"== resetting the current scene ==\")
	# remove level
	var currentLevel = get_tree().get_nodes_in_group(\"level\")
	if (!currentLevel.is_empty()):
		currentLevel = currentLevel.front()
		#get_tree().root.remove_child(currentLevel)
		currentLevel.queue_free()

	# recreate level
	create_level()


func character_ready(character):
	print(\"== character ready ==\")
	if (character_stats[\"loaded\"]):
		character.health = character_stats[\"health\"]
		character.energy = character_stats[\"energy\"]
		character.ammo = character_stats[\"ammo\"]
	else:
		character_stats[\"loaded\"] = true
	
	refresh_hud(character)


func _connect_to_character_signals(character):
	# connect character signals
	if (!character.is_connected(\"reload\", Callable(self, \"reset_current_scene\"))):
		character.connect(\"reload\", Callable(self, \"reset_current_scene\"))
	if (!character.is_connected(\"reposition\", Callable(self, \"reset_character_position\"))):
		character.connect(\"reposition\", Callable(self, \"reset_character_position\"))
	if (!character.is_connected(\"refresh_hud\", Callable(self, \"refresh_hud\"))):
		character.connect(\"refresh_hud\", Callable(self, \"refresh_hud\"))
	if (!character.is_connected(\"character_ready\", Callable(self, \"character_ready\"))):
		character.connect(\"character_ready\", Callable(self, \"character_ready\"))


func reset_character_position():
	# reset character position to start of level
	character_node.position.x = 300
	character_node.position.y = 100


func setup_character():
	print(\"== setup character ==\")
	
	if (character_node):
		character_node.remove_child(camera)
		character_node.queue_free()

	character_stats[\"loaded\"] = false
	character_node = (character_stats[\"character\"][\"scene\"]).instantiate()
	reset_character_position()
	character_node.gender = character_stats[\"character\"][\"gender\"]
	camera.enabled = true
	character_node.add_child(camera)
	_connect_to_character_signals(character_node)
	get_tree().root.add_child(character_node)
	refresh_hud(character_node)


func setup_level():
	print(\"== setup level ==\")
	if (!self.is_queued_for_deletion()):
		setup_character()


func refresh_hud(character):
	var health_bar = get_node(\"CanvasLayer/HealthProgress\")
	var energy_bar = get_node(\"CanvasLayer/EnergyProgress\")
	var ammo_indication = get_node(\"CanvasLayer/BulletCount\")
	var health_label = get_node(\"CanvasLayer/HealthLabel\")
	var energy_label = get_node(\"CanvasLayer/EnergyLabel\")
	
	print(\"== refreshing the HUD values ==\")
	# get character attributes for HUD - 
	# HUD is short for Heads Up Display. In video and computer games, 
	# the HUD is the display area where players can see their character's 
	# vital statistics such as current health, bonus attributes, armor level, ammunition count, and more.
	character_stats[\"health\"] = character.health
	character_stats[\"energy\"] = character.energy
	character_stats[\"ammo\"] = character.ammo
	character_stats[\"character\"][\"type\"] = character.type
	character_stats[\"character\"][\"gender\"] = character.gender
	
	health_label.text = str(character.health) + \"/\" + str(character.max_health)
	energy_label.text = str(character.energy) + \"/\" + str(character.max_energy)
	health_bar.value = character.health
	health_bar.max_value = character.max_health
	energy_bar.value = character.energy
	energy_bar.max_value = character.max_energy
	ammo_indication.text = \"x\" + str(character.ammo)
	
	var characterBulletImage = load(\"res://resources/characters/\" + character_stats[\"character\"][\"type\"] + \"/objects/Bullet_000.png\")
	get_node(\"CanvasLayer/BulletImage\").texture = characterBulletImage
	
	var ammo_packs = get_tree().get_nodes_in_group(\"ammo_pack\")
	if (ammo_packs.size() > 0):
		for l in ammo_packs:
			l.get_node(\"TextureRect\").texture = characterBulletImage
	
	if (!character_stats[\"loaded\"]):
		character_stats[\"loaded\"] = true
	
	print(character_stats)
"

[sub_resource type="ImageTexture" id="6"]

[node name="World" type="Node2D" unique_id=599528976]
process_mode = 3
position = Vector2(0, -2.72073)
script = SubResource("1")

[node name="CanvasLayer" type="CanvasLayer" parent="." unique_id=1116574490 groups=["status_bar"]]

[node name="HealthImage" type="TextureRect" parent="CanvasLayer" unique_id=1188412474]
offset_left = 8.73007
offset_top = 10.2581
offset_right = 38.7301
offset_bottom = 40.2581
texture = ExtResource("1")
expand_mode = 1
stretch_mode = 1

[node name="HealthProgress" type="ProgressBar" parent="CanvasLayer" unique_id=1645771780]
custom_minimum_size = Vector2(200, 0)
offset_left = 54.2301
offset_top = 10.2581
offset_right = 254.23
offset_bottom = 40.2581

[node name="EnergyImage" type="TextureRect" parent="CanvasLayer" unique_id=942051596]
offset_left = 8.73007
offset_top = 46.2581
offset_right = 38.7301
offset_bottom = 76.2581
texture = ExtResource("2")
expand_mode = 1

[node name="EnergyProgress" type="ProgressBar" parent="CanvasLayer" unique_id=646392728]
custom_minimum_size = Vector2(200, 0)
offset_left = 54.2301
offset_top = 46.2581
offset_right = 254.23
offset_bottom = 76.2581

[node name="BulletImage" type="TextureRect" parent="CanvasLayer" unique_id=1582224390]
offset_left = 256.844
offset_top = 9.23303
offset_right = 296.844
offset_bottom = 39.233
size_flags_horizontal = 0
size_flags_vertical = 0
texture = SubResource("6")
expand_mode = 1
stretch_mode = 6

[node name="BulletCount" type="Label" parent="CanvasLayer" unique_id=202211585]
offset_left = 297.844
offset_top = 19.233
offset_right = 306.844
offset_bottom = 33.233
text = "X"

[node name="GridContainer" type="GridContainer" parent="CanvasLayer" unique_id=1838438403]
offset_left = 5.84406
offset_top = -720.767
offset_right = 45.8441
offset_bottom = -680.767

[node name="EnergyLabel" type="Label" parent="CanvasLayer" unique_id=801714581]
offset_left = 53.9417
offset_top = 45.751
offset_right = 106.942
offset_bottom = 59.751
text = "100/100"
vertical_alignment = 1

[node name="HealthLabel" type="Label" parent="CanvasLayer" unique_id=1422004416]
offset_left = 53.9417
offset_top = 10.981
offset_right = 106.942
offset_bottom = 24.981
text = "100/100"
vertical_alignment = 1

[node name="PopupPanel" parent="CanvasLayer" unique_id=1897764575 groups=["popup"] instance=ExtResource("3")]
