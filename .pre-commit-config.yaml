repos:
  - repo: local
    hooks:
      - id: no-home-paths
        name: No absolute home-directory paths
        language: pygrep
        entry: '(/home/[^/]+/|C:\\Users\\[^\\]+\\)'
        exclude: '^(.*\.md|openspec/).*'
